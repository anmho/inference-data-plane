# Primary service config is YAML:
# - config/local.yaml for the default real-model MLX path
# - config/synthetic.yaml for data-plane-only load testing
#
# Set CONFIG_FILE only when you want to point the services/scripts at a
# different config document.
CONFIG_FILE=./config/local.yaml
